// const test = '';

// const obj = {
//   a: 1,
//   b: 2,
// };

const nameTest = () => {
  return '';
};

console.log(nameTest);

export const addNumbers = (a: number, b: number): number => {
  return a + b;
};

console.log(addNumbers(4, 5));
